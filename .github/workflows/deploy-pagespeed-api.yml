name: Deploy PageSpeed API

on:
  push:
    branches:
      - main
    paths:
      - "api/**"
      - "wrangler.toml"
      - ".github/workflows/deploy-pagespeed-api.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pagespeed-api-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure PageSpeed secret
        shell: bash
        env:
          PAGESPEED_API_KEY: ${{ secrets.PAGESPEED_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          test -n "$PAGESPEED_API_KEY"
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"
          printf '%s' "$PAGESPEED_API_KEY" | npx wrangler secret put PAGESPEED_API_KEY --name prftools-pagespeed-api

      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

# Deployment is triggered automatically after secret configuration changes.
